# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

*.DS_Store
.idea
.vscode
/build_assets
*.zip
*.tar.gz

# Build artifacts
go-pcap2socks
go-pcap2socks-*
go_build_*
-h

# Logs and temporary files
logs.txt
$null

# AI/editor files
CLAUDE.md
.qwen/

# WinDivert files (kept in windivert/ folder)
/windivert/*.dll
/windivert/*.sys
/windivert/*.zip

# But keep Go source files in windivert/
!/windivert/*.go

# WinDivert driver in root (runtime file, should not be tracked)
WinDivert64.sys
WinDivert.dll

# Dependencies folder (download manually)
/deps/*.exe
/deps/*.zip
/deps/*/WinDivert64.sys
/deps/*/WinDivert64.dll
/deps/*/WinDivert32.sys
/deps/*/WinDivert32.dll