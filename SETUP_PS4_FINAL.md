# 🚀 go-pcap2socks - Настройки для PS4

## ✅ Что исправлено и улучшено

### Исправления:
1. ✅ **Исправлены Toast уведомления** - больше нет ошибок PowerShell
2. ✅ **Убраны лишние уведомления** от команд службы
3. ✅ **Улучшена обработка ошибок** инициализации
4. ✅ **Добавлен graceful shutdown** для корректной остановки
5. ✅ **Улучшена обработка отключения сети**
6. ✅ **Добавлено авто-восстановление** при ошибках
7. ✅ **Улучшено логирование ошибок**

### Новые возможности:
1. ✅ **Чтение всех DHCP опций** из запроса клиента:
   - **Option 12**: Host Name (имя хоста)
   - **Option 53**: Message Type (Discover/Request)
   - **Option 55**: Parameter Request List (список запрашиваемых параметров)
   - **Option 60**: Vendor Class Identifier (идентификатор производителя)
   - **Option 61**: Client Identifier (ID клиента)

2. ✅ **Сохранение имён хостов** в DHCP leases
3. ✅ **Отображение имён устройств** в Web UI и API

---

## 📋 Быстрый старт

### 1. Запуск сервиса

**Вариант A: Через скрипт (рекомендуется)**
```cmd
run.bat
```

**Вариант B: Вручную**
1. Правый клик на `go-pcap2socks.exe`
2. **Запуск от имени администратора**

### 2. Настройка PS4

**Автоматическая (DHCP):**
1. Настройки → Настройки сети → Настроить подключение к Интернету
2. Кабель (LAN) → Автоматически

**Ручная (если автоматическая не работает):**
```
IP-адрес:     192.168.137.100
Маска:        255.255.255.0
Шлюз:         192.168.137.1
DNS 1:        8.8.8.8
DNS 2:        1.1.1.1
MTU:          1486
Прокси:       Не использовать
```

---

## 🔧 Конфигурация

Файл: `config.json`

```json
{
  "pcap": {
    "interfaceGateway": "192.168.137.1",
    "network": "192.168.137.0/24",
    "localIP": "192.168.137.1",
    "mtu": 1486
  },
  "dhcp": {
    "enabled": true,
    "poolStart": "192.168.137.100",
    "poolEnd": "192.168.137.200",
    "leaseDuration": 86400
  },
  "dns": {
    "servers": [
      {"address": "8.8.8.8:53"},
      {"address": "1.1.1.1:53"}
    ]
  },
  "routing": {
    "rules": [
      {"dstPort": "53", "outboundTag": "dns-out"},
      {"dstPort": "80,443,3478,3479,3480", "outboundTag": ""}
    ]
  },
  "outbounds": [
    {"tag": "", "direct": {}},
    {"tag": "dns-out", "dns": {}}
  ],
  "upnp": {
    "enabled": true,
    "autoForward": true,
    "gamePresets": {
      "ps4": [3478, 3479, 3480]
    }
  },
  "api": {
    "enabled": true,
    "port": 8080,
    "token": "ps4-test-token-2026"
  },
  "logging": {
    "level": "info",
    "file": "app.log"
  }
}
```

---

## 📊 Мониторинг

### Web UI
Откройте в браузере: **http://localhost:8080**

### API запросы

```powershell
# Статус сервиса
curl http://localhost:8080/api/status -H "Authorization: Bearer ps4-test-token-2026"

# Устройства (с именами хостов!)
curl http://localhost:8080/api/devices -H "Authorization: Bearer ps4-test-token-2026"

# DHCP leases (с именами хостов, Vendor Class, Parameter List)
curl http://localhost:8080/api/dhcp/leases -H "Authorization: Bearer ps4-test-token-2026"

# Трафик
curl http://localhost:8080/api/traffic -H "Authorization: Bearer ps4-test-token-2026"

# Логи
curl http://localhost:8080/api/logs -H "Authorization: Bearer ps4-test-token-2026"
```

---

## 🎮 Что читается из DHCP запроса PS4

| Option | Название | Пример | Описание |
|--------|----------|--------|----------|
| **12** | Host Name | "PS4-123" | Имя хоста устройства |
| **53** | Message Type | 1=Discover, 3=Request | Тип DHCP сообщения |
| **55** | Parameter Request | [1,3,6,15,28,...] | Список запрашиваемых параметров |
| **60** | Vendor Class | "MSFT 5.0" или "PSPC" | Идентификатор производителя |
| **61** | Client ID | Уникальный ID | Идентификатор клиента |

**PS4 сама решает, какие параметры использовать** из тех, что отправляет сервер в DHCP OFFER/ACK.

---

## ❓ Решение проблем

### Сервис не запускается
1. Убедитесь, что запущен **от имени администратора**
2. Проверьте, что установлен **Npcap**: https://npcap.com
3. Проверьте, что **кабель Ethernet подключён**
4. Проверьте логи в `app.log`

### PS4 не видит сеть
1. Проверьте, что кабель подключён
2. Проверьте ARP таблицу: `arp -a | findstr 192.168.137`
3. PS4 должна быть: `192.168.137.100  78-c8-81-4e-55-15`

### NAT тип 3 (строгий)
1. Включите UPnP на роутере
2. Или пробросьте порты вручную:
   - **3478-3480 TCP/UDP** → `192.168.137.100`

### Ошибка "network adapter disconnected"
1. Проверьте, что кабель Ethernet подключён
2. Проверьте, что сетевой адаптер включён
3. Перезапустите сервис

---

## 📞 Поддержка

- **Web UI**: http://localhost:8080
- **Логи**: `app.log`
- **Telegram бот** (если включён): `/status`, `/devices`

---

## 📈 История изменений

### v3.19.12+ (26 марта 2026)
- ✅ Исправлены Toast уведомления (PowerShell errors)
- ✅ Убраны лишние уведомления от команд службы
- ✅ Улучшена обработка ошибок инициализации
- ✅ Добавлено чтение всех DHCP опций (Hostname, Vendor Class, etc.)
- ✅ Добавлено сохранение имён хостов в DHCP leases
- ✅ Улучшена обработка отключения сети
- ✅ Добавлен graceful shutdown
- ✅ Улучшено логирование ошибок

---

**Версия:** 3.19.12+  
**Последнее обновление:** 26 марта 2026 г.
